# requirements.txt for evaluate.py ONLY (inference, not training)
#
# Deliberately minimal — evaluate.py imports exactly these three packages
# and nothing else. It does NOT need lpips / pytorch-msssim / ipywidgets,
# since it only runs inference and writes files, it doesn't score anything.
# Keeping those out removes the exact runtime-download risk flagged for
# LPIPS's pretrained weights.
#
# `pillow` is only actually touched if the test set turns out to be image
# files (.png/.tif/.jpg/.bmp) rather than .npy — but it's cheap to include
# either way so the script never breaks on an unexpected format.
#
# torch/numpy versions below are UNPINNED on purpose: I have no way to
# verify exact versions against your real training environment from here.
# Before you submit, replace the two lines below with the exact torch/numpy
# versions from your actual Kaggle training session:
#     pip freeze | grep -iE "^(torch|numpy|pillow)=="
# and paste those pinned lines in here instead.

torch
numpy
pillow

# ------------------------------------------------------------------
# NOTE ON THE FULL SUBMISSION requirements.txt
# ------------------------------------------------------------------
# The problem statement asks for the COMPLETE `pip freeze` output from your
# training environment (deliverable #5), not just these three packages.
# That has to come from your actual Kaggle notebook session — I can't
# fabricate real version numbers for lpips/pytorch-msssim/torch/etc. from
# here without risking wrong pins. Once training is final, run this in
# that same Kaggle session and use ITS output as your submitted file:
#
#     pip freeze > requirements.txt
#
# As long as that freeze includes compatible torch/numpy/pillow versions,
# evaluate.py will run fine against it — it just won't use the training-only
# packages (lpips, pytorch-msssim, ipywidgets) that also get captured.
